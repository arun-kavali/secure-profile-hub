# =============================================================================
# REQUIRED VARIABLES
# =============================================================================

# Database (NeonTech PostgreSQL or local PostgreSQL)
# Format: postgresql://username:password@host:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@localhost:5432/myapp

# JWT Authentication
# Generate a secure random string (32+ characters recommended)
# You can change this anytime - existing users will need to re-login
JWT_SECRET=your-super-secret-jwt-key-min-32-chars-change-this
JWT_EXPIRES_IN=7d

# Media Base URL (where uploaded images are served from)
# For local development: http://localhost:3001/uploads
# For Vercel deployment: https://your-backend.vercel.app/uploads
# For AWS CloudFront: https://your-distribution.cloudfront.net
MEDIA_BASE_URL=http://localhost:3001/uploads

# =============================================================================
# OPTIONAL: AWS S3 + CloudFront (for production)
# =============================================================================
# If these are NOT set, the backend uses local file storage automatically.
# To enable S3 storage, provide ALL four AWS variables below.

# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=ap-south-1
# AWS_BUCKET_NAME=your-bucket-name

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

PORT=3001
NODE_ENV=development

# Frontend URL for CORS (optional, defaults to allow all origins)
FRONTEND_URL=http://localhost:5173
